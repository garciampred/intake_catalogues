sources:
  jasmin:
    args:
      obj: "{{CATALOG_DIR}}/jasmin/jasmin-catalogue.json"
    description: This catalog contains datasets for EERIE accessible on JASMIN
    driver: intake.open_esm_datastore
  dkrz:
    args:
      path: "{{CATALOG_DIR}}/dkrz/main.yaml"
    description: This catalog contains datasets for EERIE accessible on DKRZ
    driver: yaml_file_cat
