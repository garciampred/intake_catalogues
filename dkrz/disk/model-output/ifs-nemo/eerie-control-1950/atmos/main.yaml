sources:
  native:
    args:
      path: "{{CATALOG_DIR}}/native/main.yaml"
    description: This catalog contains atmospheric EERIE IFS-NEMO eerie-control-1950 output on 0.25deg grid available at DKRZ disk.
    driver: yaml_file_cat

